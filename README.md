## Limitations

- Sentiment140 is a legacy dataset (2009) and may not reflect modern policy discourse.
- Keyword filtering reduces dataset size and may introduce class imbalance.
- The RAG module uses semantic similarity retrieval without a generative LLM.